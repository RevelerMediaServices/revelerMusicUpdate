import styled from "styled-components";

const BackOfBusCardDiv = styled.div`
  color: white;
`;

function BackOfBusCard() {
  return <BackOfBusCardDiv>Back of Bus Card</BackOfBusCardDiv>;
}

export default BackOfBusCard;
